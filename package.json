{
  "name": "prevent-fixup-commits",
  "version": "1.0.0",
  "description": "GitHub Action to prevent merging pull requests containing \"fixup!\" commits:",
  "main": "dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "ncc build -o dist index.ts",
    "run": "ncc run index.ts",
    "format": "dprint fmt",
    "format:check": "dprint check",
    "release": "standard-version"
  },
  "packageManager": "pnpm@8.15.7",
  "license": "MIT",
  "devDependencies": {
    "@vercel/ncc": "^0.38.1",
    "dprint": "^0.45.1",
    "standard-version": "^9.5.0",
    "typescript": "^5.4.5"
  },
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/github": "^6.0.0"
  }
}
